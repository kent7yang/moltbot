[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "npx clawdbot gateway start --bind 0.0.0.0"
healthcheckPath = ""
healthcheckTimeout = 0
restartPolicyType = "ON_FAILURE"
